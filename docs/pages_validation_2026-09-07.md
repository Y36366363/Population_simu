# GitHub Pages 验证记录（2026-09-07）

公开站点 `https://Y36366363.github.io/Population_simu/` 已返回 HTTP 200，页面标题为
“家庭迁徙与世界发展沙盘 · Population Simu”。页面引用的 `styles.css`、`app.js`、
`favicon.svg` 和首页相对路径均返回 HTTP 200。

本地验证：

```text
static_site_ok assets=5
114 passed
```

研究数据验证：ACS 住房面板 550 行、50 州、无重复键；四模型 rolling-origin 可以运行。
当前网页部署成功只说明静态资源可访问，不代表 WONDER 分层出生分子或正式历史回放已经完成。
